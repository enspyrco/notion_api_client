part of 'block_type.dart';

/// Breadcrumb blocks
class BreadcrumbBlock extends BlockType {
  /// Breadcrumb block objects do not contain any information within the breadcrumb property
  BreadcrumbBlock.fromJson(JsonMap json);
}
