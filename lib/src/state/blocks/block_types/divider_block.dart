part of 'block_type.dart';

/// Divider blocks
class DividerBlock extends BlockType {
  /// Divider block objects do not contain any information within the divider property
  DividerBlock.fromJson(JsonMap json);
}
